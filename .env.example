# AWS Configuration
AWS_REGION=us-east-1

# OpenSearch Configuration
# Leave empty to use local Docker OpenSearch instance
OPENSEARCH_ENDPOINT=

# Optional: API Configuration
# API_KEY=dev-key

# Optional: CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000
